apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: auth-ns
  labels:
    app: payment-service
    monitoring: enabled
spec:
  selector:
    app: payment-service
  ports:
  - name: http
    port: 80
    targetPort: 8080
  - name: metrics
    port: 8001
    targetPort: 8001
